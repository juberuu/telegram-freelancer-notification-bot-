# Validation — September 11, 2026


- Latest revision: all 96 tests pass with h2 titles, native section dividers, and compact skill rows without double line breaks. Pixel-exact sizing, custom backgrounds, and reaction-style skill badges are not implemented because Telegram message formatting does not expose those controls. Live visual rendering remains unverified.

- Latest layout cleanup: all 96 tests pass. Regressions verify plain escaped/deduplicated skills, compact skill rows, two-row metadata, and absence of quote boxes, highlights, buttons, dividers, and double breaks. Telegram rendering has not been live-verified after this change; earlier styling notes below describe superseded versions.

- All 96 automated tests pass. Current layout regressions cover two metadata rows, separate icon headings, padded skill rows, native highlights instead of disabled buttons, and a final post divider. Actual message-bubble spacing and highlight colors depend on Telegram; this update was not visually verified in a live chat. New callback coverage checks both F/H explanations and private-chat authorization; skill assertions now validate escaped native primary button labels. Client verification uses the same native quote block as metadata and skills. Exact theme rendering and hover behavior have not been live-tested; hover tooltips are unsupported and tap explanations are implemented instead. Skill badge tests cover escaping, deduplication, unknown omission, and safe standard-HTML fallback. Title and metadata assertions cover h3, budget/time/bid emoji icons, country flag plus name, the ordered single logical metadata row, native divider/quote blocks, and spaced standard-HTML dividers. Live rendering of this styling update has not been verified. New tests verify that blocked avatar work cannot consume public verification workers, two subscribers share one successful concurrent public lookup, and exhausted public retries still schedule the owner API fallback. Live end-to-end latency was not measured in this update. Concurrent enrichment is tested with a blocked lookup and a second post in the same chat; the second completes without waiting for the first. Layout assertions cover the compact editorial E design, normal-size linked title, grouped header rows, inline Client/Skills labels, and native verification emoji icons. These checks use mocked Telegram calls; the revised layout has not been visually verified in a live chat. Additional regressions cover client updates despite pending alerts, publishing public states before a failing avatar lookup, restart recovery, and safely escaped clickable titles with no brief or buttons in both layouts. New delivery tests cover live alerts without client-lookup waits, overlapping sends to different chats, prevention of duplicate in-flight delivery to one subscriber, per-chat pacing, exact scan-start cadence, cached-state freshness, short transient retries, malformed API responses, and priority over summaries. Existing tests cover rolling statistics, subscriber isolation, persistence, filters, avatar edits, and rate-limit cooldowns.
- The delivery investigation found saved connection failures from both Freelancer and Telegram, with pending alerts aging past the five-minute cutoff. Read-only health checks subsequently succeeded for the Freelancer listing endpoint and Telegram `getMe`, consistent with intermittent failures. No project messages were sent during this investigation. Production must be restarted to load the fixes; source availability and Telegram acceptance can still delay or prevent alerts.
- The statistics component uses a native compact Telegram table with a text fallback. Tests use mocked Telegram delivery; no live statistics message was sent. History begins after restart and the first successful source scan, with longer windows marked partial until enough history exists.
- Subscription delivery was tested using simulated private chats and mocked Telegram calls. No messages were sent to other users during this implementation. Restart the host process to activate the subscription coordinator; users then send `/start` and `/skills` in their own chat with the same bot.
- The owner's existing database is retained. Other subscribers receive separate databases beside it, and the owner database holds the subscriber registry.

## Prior avatar and layout checks — September 9, 2026

- All 55 automated tests pass, including owner-specific avatar lookup, missing/wrong owner rejection, cache and cooldown behavior, rich photo payloads, background message edits, and delivery fallback behavior.
- Read-only live check: the latest saved project (40700239) supplied five client verification states on its public page but no avatar. Its project-details lookup returned HTTP 404. Three active listings sampled from the API omitted all owner IDs and user records. No Freelancer OAuth token was configured.
- Photo display is implemented using Telegram's documented rich-message media API. The standard text layout is retained for definite API rejections. Tests validate payloads with mocked Telegram calls; actual Telegram rendering and a real photo send were not verified in this update.
- Restart the running bot to load the edited Python files. Existing delivered posts are not automatically replayed; `/latest` requests new previews within the configured age window.

## Original baseline — September 7, 2026

- Python 3.12: all 13 automated tests passed.
- Python compilation check passed for the application and setup/check scripts.
- Offline synthetic alert preview matched the shipped default filters.
- Read-only live Freelancer.com check: 37 recent projects retrieved, 6 matched the shipped default filters. These counts are a point-in-time observation, not a promise of future alert volume.
- The public response omitted client details. Optional client-country and payment-verification filters exclude unknown values, and `/status` reports this limitation.
- Telegram pairing, real Telegram message delivery, the Windows launcher, and Docker deployment were not executed in this environment. A bot token and the user's private Telegram interaction are needed to complete activation. Docker was not installed in the build environment.

Tests use synthetic fixtures and a simulated Telegram sender. The live source check contacted only the Freelancer projects endpoint and did not send messages, place bids, or contact clients.
